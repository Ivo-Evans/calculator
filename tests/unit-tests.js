console.group('unit tests')
console.group('parse')
test('The parser ignores whitespace', (assert) => {

})

test('The parser ignores non-number or operator characters',(assert) => {

})

test('The parser detects exponents', (assert) => {

})

test('The parser detects parentheses', (assert) => {

})

test('The parser matches the outer layer of nested parentheses', (assert) => {

})

test('The parser matches adjacent parentheses separately', (assert) => {

})
console.groupEnd('parse')
console.groupEnd('unit tests')